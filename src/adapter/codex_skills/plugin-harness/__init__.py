"""Marker package — the deepest level of the bundled SKILL.md package
tree. Without this `__init__.py`, `importlib.resources.files(
"src.adapter.codex_skills.plugin-harness")` cannot resolve.

PR #26 LLM review (🟠 major #3).
"""
